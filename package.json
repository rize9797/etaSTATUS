{
  "name": "etastatus",
  "version": "1.0.0",
  "description": "etaSTATUS: PS5 and PS4 firmware history with PSN sign-in status per version.",
  "type": "module",
  "scripts": {
    "update": "node fetch-live.mjs",
    "serve": "npx --yes serve@14 . -l 8080",
    "check": "node --check app.js && node --check fetch-live.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
